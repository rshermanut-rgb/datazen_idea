# DataZen Landing Page Prototype

Upload `index.html` to the root of your GitHub Pages repository.

Because the page is self-contained, the images/video assets are embedded directly in the HTML file.
